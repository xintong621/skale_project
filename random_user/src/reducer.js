const initialState = {
  users: [],
  userDetail: null,
  loading: false
};

function myApp(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}


export default myApp;
