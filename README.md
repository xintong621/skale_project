### Show the information of some random users

data source: https://randomuser.me/

* put the folder in your local machine
* open the folder
* run `npm install`
* run `npm start`
* try the app on localhost:3000
